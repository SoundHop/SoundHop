<Window
    x:Class="AudioSwitcher.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:AudioSwitcher.UI"
    xmlns:converters="using:AudioSwitcher.UI.Converters"
    xmlns:models="using:AudioSwitcher.Core.Models"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid">
        <!-- Tray Icon Context Menu -->
        <Grid.ContextFlyout>
            <MenuFlyout x:Name="TrayContextMenu">
                <MenuFlyoutItem x:Name="SettingsMenuItem" Text="Settings" Click="OnSettingsClick">
                    <MenuFlyoutItem.Icon>
                        <FontIcon Glyph="&#xE713;" FontFamily="Segoe Fluent Icons"/>
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
                <MenuFlyoutSeparator/>
                <MenuFlyoutItem x:Name="ExitMenuItem" Text="Exit" Click="OnExitClick">
                    <MenuFlyoutItem.Icon>
                        <FontIcon Glyph="&#xE7E8;" FontFamily="Segoe Fluent Icons"/>
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
            </MenuFlyout>
        </Grid.ContextFlyout>
        
        <local:MainPage x:Name="FlyoutPage"/>
    </Grid>

</Window>
