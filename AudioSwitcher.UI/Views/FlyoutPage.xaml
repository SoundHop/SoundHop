<Page
    x:Class="AudioSwitcher.UI.Views.FlyoutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:AudioSwitcher.UI.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <controls:DeviceListControl x:Name="DeviceListControl" Grid.Row="0" IsCompact="True"/>
        
        <!-- Open Dashboard Button -->
        <Button Grid.Row="1" 
                Click="OpenDashboard_Click"
                HorizontalAlignment="Stretch"
                Margin="8,4,8,8"
                Padding="12,8"
                Background="Transparent"
                BorderThickness="0">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <FontIcon Glyph="&#xE8A7;" FontSize="14" 
                          Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                <TextBlock Text="Open dashboard" 
                           Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
            </StackPanel>
        </Button>
    </Grid>
</Page>
