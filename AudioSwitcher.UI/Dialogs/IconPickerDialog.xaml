<ContentDialog
    x:Class="AudioSwitcher.UI.Dialogs.IconPickerDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Choose Icon"
    PrimaryButtonText="OK"
    SecondaryButtonText="Reset"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick"
    CloseButtonText="Cancel"
    DefaultButton="Primary">

    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" MaxHeight="400">
        <GridView x:Name="IconGridView" 
                  SelectionMode="Single"
                  ItemsSource="{x:Bind Icons}"
                  SelectionChanged="IconGridView_SelectionChanged"
                  Padding="4"
                  HorizontalAlignment="Center">
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid Orientation="Horizontal" MaximumRowsOrColumns="5" ItemWidth="64" ItemHeight="64"/>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Border Width="52" Height="52" 
                            CornerRadius="4"
                            Background="Transparent"
                            ToolTipService.ToolTip="{Binding Name}">
                        <FontIcon Glyph="{Binding Glyph}" 
                                  FontFamily="Segoe Fluent Icons" 
                                  FontSize="22"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
                    </Border>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </ScrollViewer>
</ContentDialog>
